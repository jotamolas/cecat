{% extends "AppBundle:Default:admin.html.twig"%}
{% block action_content %}

    <div class="card">
        <div class="card-header">Registro Registration</div>
        <div class="card-body">
            {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}                    
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}   
