{% extends "AppBundle:Default:admin.html.twig"%}
{% block title %}
    Registrar Servicio
{% endblock %}
{% block action_content %}


    <div class="card">
        <div class="card-header">Registrar Servicio</div>
        <div class="card-body">
            {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}  
            {{ form_start(form) }}
            <div class="row">
                <div class="col-sm-8 ">
                    <div class="form-group">
                        {{ form_errors(form.description) }}
                        <label>Descripción</label>
                        {{ form_widget(form.description) }}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-8">
                    <div class="form-group">
                        {{ form_errors(form.price) }}
                        <label>Costo</label>
                        {{ form_widget(form.price) }}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-8">
                    <input type="submits" class="btn btn-outline-primary btn-sm" value="Grabar"/>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
